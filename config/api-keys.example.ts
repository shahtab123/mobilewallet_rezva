/**
 * Copy this file to `api-keys.ts` (gitignored) and fill in local keys.
 * Existing Ankr RPC usage in this wallet is optional for Polygon history.
 */
export const API_KEYS = {
  ANKR_API_KEY: '',
};
